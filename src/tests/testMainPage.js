/*const app = require ('../app/app');

describe ('Main page', function(){
    it ('UI: main page elements', async function(){

        await app.ui.header.click('cookies');
        await app.ui.welcomeModuleClose()
        //await app.ui.emailSubscrClose();

        const texTrend = await app.ui.mainPage.getText('trending');
        app.expect(texTrend).to.equal('Trending now');

        const texBestSel = await app.ui.mainPage.getText('bestSellers');
        app.expect(texBestSel).to.equal('Best sellers');

        const texNew = await app.ui.mainPage.getText('newArrivals');
        app.expect(texNew).to.equal('New arrivals');
       

        // await app.ui.mainPage.clickBy('customBanner', 'VITABLES');
        // await app.ui.ph.page.waitForResponse(resp => {
        //     return resp.url() === 'https://www.iherb.com/cl/vitables' && resp.status() === 200;
        // })
        
        // await app.ui.productCont.clickBy('panelItems', 0);
        // await app.ui.ph.page.waitFor(5000);
        // await app.ui.productCont.click('addToCart');

    });

    // it ('API:', async function(){
    
    // });

});*/